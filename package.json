{
  "name": "vibeify-forge",
  "version": "0.1.0",
  "description": "Browser-based prompt design and library plugin",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "preview": "vite preview",
    "prepare-engine": "cd node_modules/@vibeify/engine && npm install && npm run build",
    "postinstall": "npm run prepare-engine || true"
  },
  "dependencies": {
    "@vibeify/engine": "file:../vibeify-engine"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vite": "^7.3.1",
    "vite-plugin-static-copy": "^3.1.4"
  }
}
